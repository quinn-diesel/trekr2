<%= form_for @hike, html: {multipart: true} do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>

  <%= f.label :description %>
  <%= f.text_field :descrption %>
  <br>

  <%= f.submit %>

<% end %>

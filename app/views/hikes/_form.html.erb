<%= form_for @hike, html: {multipart: true} do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>

  <%= f.label :description %>
  <%= f.text_field :description %>
  <br>

  <%= f.submit 'Update Hike Path', :class => "waves-effect waves-light btn", :id => 'updateHike' %>

<% end %>

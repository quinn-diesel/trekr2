<script>
  var waypoints = <%= raw @waypoints.to_json %>;

  var hike_id = '<%= @hike.id %>';

  var markers = <%= raw @hike.markers.to_json include: [:tips] %>

  var current_user = <%= raw @current_user.to_json %>

  // var tips = <%=  %>

</script>

<div id="mapid"></div>

<!-- <button type="button" id="addMarker">Add Marker</button> -->

<div>
    <% if @current_user == @hike.user %>
       <div id='editHike' ><%= link_to 'Edit Hike', edit_hike_path(@hike) %></div>
    <% end %>
    <h3><%= @hike.name %></h3>
    <p><%= @hike.description %></p>
</div>

<div class="fixed-action-btn horizontal">
  <a class="btn-floating btn-large red">
    <i class="large material-icons">mode_edit</i>
  </a>
  <ul>
    <li><a class="btn-floating red"><i class="material-icons" id="addMarker">location_on</i></a></li>
    <li><a class="btn-floating yellow darken-1"><i class="material-icons">format_quote</i></a></li>
  </ul>
</div>



<div id='commentContainer'>
    <% @hike.comments.each do |c| %>
            <p id="userName"> <%= @current_user.firstname %> <%= @current_user.surname %> said...</p>
            <p id="comment"><%= c.comment %></p>
    <% end %>
</div>
<textarea id="commentText"placeholder="Comment" rows="8" cols="80"></textarea>
<button id="submitComment">Submit</button>



<!-- # <%= form_for new_tip_path, html: {multipart: true} do |f| %>
#
#   <%= f.label :tip %>
#   <%= f.text_field :tip %>
#   <br>
#
#   <%= f.submit %>
#
# <% end %> -->
